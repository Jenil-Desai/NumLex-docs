---
title: Probability (prob.py)
description: Functions for calculating permutations and combinations in NumLex
---

import { Code } from "@astrojs/starlight/components";

The **Probability** module in NumLex includes functions for calculating permutations and combinations.

## Functions

- **`perm(n, r)`**: Calculates the number of permutations.

  <Code code={`perms = perm(n=5, r=3)`} lang="python"></Code>

- **`comb(n, r)`**: Calculates the number of combinations.

  <Code code={`combs = comb(n=5, r=3)`} lang="python"></Code>
